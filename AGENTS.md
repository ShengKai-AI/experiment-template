# 项目规则

## 基本原则

- 本仓库是项目代码、实验配置、运行过程和实验结果的共同事实来源，不依赖对话历史保存关键信息。
- 仓库中新增或修改的注释统一使用中文，包括代码注释和配置文件注释。
- 所有 Run、Execution、Stage、日志和通知时间统一使用北京时间；结构化记录采用带 `+08:00` 偏移的 ISO 8601 格式。
- 不得覆盖、删除或混入与当前任务无关的用户修改。
- 优先保持结构简单；新增顶层模块或调整核心架构前，先说明必要性和影响范围。

## 核心概念

- Project：整个 GitHub 仓库。
- Experiment：一份独立、可复用的计划配置。
- Run：一次运行活动，是进度记录和可视化监控的对象，可以包含多个 Execution。
- Execution：某份 Experiment 在一个 Run 中的一次实际执行。
- Stage：Execution 中由项目自行定义的执行阶段。
- Framework Code：多个实验共享的代码，存放在 `src/framework/`。
- Experiment Code：具体实验代码，按类型存放在 `src/experiments/` 下。

## 工作模式

每个任务开始时，Agent 必须判断当前工作模式，并读取对应的模式文件：

- 涉及架构、代码、接口、工具、依赖、测试或调试时，进入工程模式并读取 `AGENTS/engineering.md`。
- 涉及实验计划、Experiment 配置、Run、实验执行、运行监控或结果汇报时，进入实验模式并读取 `AGENTS/experiment.md`。
- 用户明确指定模式时，以用户指定为准。
- 同时涉及两种模式时，先拆分任务阶段并说明当前模式；切换模式时重新读取对应文件。

## 共同工作要求

Agent 在修改代码或运行实验前必须：

1. 阅读适用的模式文件、相关配置和未完成记录。
2. 检查当前 Git 分支、commit 和工作区状态。
3. 明确本次任务的输入、预期输出和完成条件。
4. 确认不会覆盖已有 Run、结果或用户修改。

## Git 与任务交付

- `main` 保存稳定状态；不同实验族可以使用独立实验分支，但具体分支名应服从项目约定。
- 不得未经用户要求重写 Git 历史或执行破坏性操作。
- 完成任务时，向用户报告完成内容、关键文件、验证结果、Git 状态、产物路径和未完成事项。
