# 项目规则

## 基本原则

- 本仓库是项目代码、实验配置、运行过程和实验结果的共同事实来源，不依赖对话历史保存关键信息。
- 仓库中新增或修改的注释统一使用中文，包括代码注释和配置文件注释。
- 不得覆盖、删除或混入与当前任务无关的用户修改。
- 优先保持结构简单；新增顶层模块或调整核心架构前，先说明必要性和影响范围。

## 核心概念

- Project：整个 GitHub 仓库。
- Experiment：一份独立、可复用的计划配置。
- Run：一次运行活动，是进度记录和可视化监控的对象，可以包含多个 Execution。
- Execution：某份 Experiment 在一个 Run 中的一次实际执行。
- Stage：Execution 中由项目自行定义的执行阶段。
- Framework Code：多个实验共享的代码，存放在 `src/framework/`。
- Experiment Code：具体实验代码，按类型存放在 `src/experiments/` 下。

## 开始工作前

Agent 在修改代码或运行实验前必须：

1. 阅读适用的 `AGENTS.md`、相关 Experiment 配置和未完成的 Run。
2. 检查当前 Git 分支、commit 和工作区状态。
3. 判断任务属于框架修改、实验代码修改、配置修改还是实际运行。
4. 确认不会覆盖已有 Run、结果或用户修改。

## 参数与运行权限

- 已有配置明确给出的参数可以直接使用。
- 未经用户或已有配置明确指定时，Agent 不得自行决定会影响实验语义或可比性的设置，包括实验目标、对照关系、模型、数据、随机种子、训练与推理参数、预算、评测方法和指标。
- Agent 可以自行处理不改变实验语义的 ID、时间戳、日志名、输出路径和格式整理。
- 所有可能影响结果的运行时调整都必须记录实际值、原值、原因和时间。
- 开始消耗较多时间、GPU、API 配额或其他资源的正式实验前，必须获得用户明确授权。

## Experiment 管理

- 主实验、基线和消融配置分别存放在 `experiments/main/`、`experiments/baselines/` 和 `experiments/ablations/`。
- `experiments/example.yaml` 仅用于展示结构，不得作为真实实验配置使用。
- Experiment 使用固定的 `experiment` 信息和项目自由定义的 `spec`；详细形式以示例文件为准。
- Experiment 记录计划执行的内容，不记录某次运行的动态状态。
- 正式运行前不得保留会影响执行的未确认占位值。
- 已被 Run 使用的 Experiment 如果发生语义变化，必须增加版本或创建新配置；不得静默改变历史含义。

## Run 与 Execution 管理

- `runs/example.yaml` 仅用于展示结构，不得改写为真实运行记录。
- 真实 Run 存放在 `runs/<run_id>/run.yaml`，一般使用时间戳和简短名称组成 `run_id`。
- Run 必须记录时间、状态、Git commit、分支、工作区状态以及包含的所有 Execution。
- 每个 Execution 必须引用源 Experiment，并保存开始执行时实际使用的完整 `config_snapshot`。
- 创建快照时，应先应用已经确认的参数调整；差异同时记录在 `adjustments` 中。
- Execution 开始后不得静默修改 `config_snapshot`。改变实验语义时创建新的 Execution；恢复性或操作性调整可以保留在原 Execution，但必须追加记录。
- Run、Execution 和 Stage 的状态必须反映实际情况。不得仅根据计划、进程存在或局部日志将其标记为完成。

## Stage、输入与输出

- Stage 的名称和结构由具体项目定义，预计阶段优先在 Experiment 中声明。
- 每个实际 Stage 应记录状态、时间、实际命令、输入、输出、日志和必要指标；不适用的字段可以省略。
- 关键数据、模型和结果应记录明确路径，并在可行时记录版本、commit 或哈希。
- Stage 开始和结束时应及时更新记录；没有生成必需输出的 Stage 不得标记为完成。
- 运行时新增 Stage 时，必须记录新增原因。

## 结果与事实

- 只记录实际观测结果，不得用目标值、推测值、临时指标或不完整评测代替最终结果。
- 实验失败、暂停或结果不完整时必须如实记录原因和当前状态。
- 明确区分代码实现、配置验证、冒烟测试、局部实验、正式实验和正式评测。
- 对外报告应给出实际指标、评测条件、结果路径以及缺失或异常情况。

## 代码与 Git

- 共享逻辑放入 Framework Code；实验专用逻辑放入对应的 Experiment Code，避免复制共享实现。
- 一个 Project 只使用一个 GitHub 仓库。实验目录负责分类，Git 分支负责隔离代码演化，两者可以同时使用。
- `main` 保存稳定状态；不同实验族可以使用独立实验分支，但具体分支名应服从项目约定。
- Run 必须记录实际 commit，不能只记录分支名。
- 正式 Run 优先使用干净且已提交的代码；如必须使用未提交代码，应记录工作区状态或保存补丁信息。
- 不得未经用户要求重写 Git 历史或执行破坏性操作。

## 完成任务时

- 执行与修改风险相称的验证。
- 更新相关 Experiment、Run、Execution 和 Stage 的实际状态。
- 向用户报告完成内容、关键文件、验证结果、Git 状态、产物路径和未完成事项。
- 不得把计划行为描述为已经完成的事实。
